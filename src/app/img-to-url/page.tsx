'use client';

export default function ImgToUrlPage() {
  // The actual uploader layout is persistently rendered at the layout level (MobileMainWrapper) 
  // to preserve uploader history/session state when navigating between routes.
  return <div style={{ height: '100%' }} />;
}
